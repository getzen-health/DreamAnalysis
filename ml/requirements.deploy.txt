# Inference-only requirements for cloud deployment.
# Excludes: torch (denoiser optional), brainflow (hardware, not available in cloud),
#           training libs (moabb, kaggle, etc.), data libs (h5py, boto3),
#           DB libs (psycopg2, asyncpg), monitoring (ddtrace).

fastapi==0.115.0
uvicorn[standard]==0.30.6
numpy==1.26.4
scipy==1.13.1
scikit-learn==1.5.2
PyWavelets==1.7.0
onnxruntime==1.19.2
pydantic==2.9.2
python-multipart==0.0.12
httpx==0.27.2
lightgbm>=4.0.0
joblib>=1.3.0
pandas>=2.0.0
websockets==12.0
