# ── Runtime dependencies for the FastAPI ML server ──────────────────────────
# This is a subset of requirements.txt — only what's needed to RUN the server.
# Training/dataset packages (moabb, openneuro-py, kaggle, etc.) are excluded
# because they take a long time and some fail on Python 3.13.
#
# Used by: ml/start.sh
# For full training environment: pip install -r requirements.txt

# Server
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
python-multipart==0.0.12
httpx==0.27.2
websockets==12.0

# EEG hardware
brainflow==5.12.1

# Signal processing + ML
numpy>=1.26.4
scipy==1.13.1
scikit-learn==1.5.2
PyWavelets==1.7.0
joblib>=1.3.0
lightgbm>=4.0.0
pandas>=2.0.0
h5py>=3.0.0

# Inference
onnxruntime==1.19.2
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.4.1

# Database (optional — for session storage)
psycopg2-binary>=2.9.9
asyncpg>=0.29.0
