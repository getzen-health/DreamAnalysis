# Research Learnings — NeuralDreamWorkshop

> Auto-maintained by the research agent. Each entry is a specific finding from a research cycle.
> Format: Date | Topic | Finding | Source | Action Taken

---

## Last updated: 2026-03-20

| Date | Topic | Finding | Action |
|---|---|---|---|
| 2026-03-19 | ux-polish | Sleep page SleepDataTab used static divs with no entry animations or animated fills, unlike mood/stress/focus pages which all use motion.div + cardVariants + animated gauges/bars. Inconsistent motion design breaks the premium feel. | Added staggered cardVariants entry animations to all three sleep data cards and animated the sleep stages stacked bar segments (width 0 -> final width with staggered delay). Matches the animation language used across mood-trends, stress-trends, and focus-trends. |
| 2026-03-19 | medical-analytics | Breathing (`/breathing/prescribe`) and music therapy (`/music-therapy/prescribe`) endpoints used the regulated term "prescribe" without any wellness disclaimer, while other clinical-adjacent modules (seizure, perinatal, psychedelic, voice biomarkers, cognitive) all included disclaimers. FDA SaMD risk: seizure detector is Class II, attention screening could be SaMD if marketed for ADHD. PHQ-9/GAD-7 already in clinical_bridge.py but needs clear distinction from EEG-derived mood estimates. | Added `_WELLNESS_DISCLAIMER` to breathing and music therapy route responses. Updated docstrings to say "recommend" instead of "prescribe." Created issue #463 for full medical claims audit covering FDA SaMD, HIPAA, evidence grading, and remaining endpoint disclaimers. |
| 2026-03-19 | eeg-brain | Gamma band upper bound was 100 Hz in BANDS dict, `get_personalized_bands()`, and `meditation_depth.py`, but the preprocessing bandpass filter cuts at 50 Hz. Welch PSD above 50 Hz is near-zero, so integrating 30-100 Hz artificially diluted gamma power estimates. Other modules (graph_emotion, cnn_kan, dsp_mcf, etc.) already used 45 or 50 Hz caps independently. | Capped gamma to (30, 50) Hz in `eeg_processor.py` BANDS, `get_personalized_bands()`, and `meditation_depth.py`. Updated test assertion in `test_dual_epoch_iaf.py`. All 22 IAF tests pass. |
| 2026-03-19 | human-emotion | Emotion regulation module (`emotion_regulation.py`) only provided cognitive reappraisal strategies across all emotional states. Per Gross (2015) process model and Sheppes et al. (2011), under high arousal negative states, distraction and acceptance outperform reappraisal because reappraisal requires cognitive resources depleted by stress. The `_STRATEGIES` dict had no strategy-type labels, making it impossible to match the right regulation approach to the user's state. Also found: emotion wheel component uses labels (relaxed, focused) that don't match the classifier output (surprise, neutral), though the component is not actively imported in any page. | Added typed strategy dicts with 4 evidence-based types (cognitive_reappraisal, acceptance, distraction, physiological) keyed by emotional state. High-arousal negative states now lead with distraction/acceptance. Updated test to validate strategy type labels. All 78 tests pass. |
| 2026-03-20 | health-analysis-app | Competitive analysis was missing Oura, Whoop, Calm, and Headspace — the highest-retention consumer health apps. All top-retained apps (Oura ~65%, Whoop ~55%) share one pattern: a single daily composite score users check every morning. NDW has the richest data (EEG + voice + sleep + HRV + food) but no single "How am I today?" number. This is the #1 retention gap. Oura/Whoop also drive growth via shareable score cards (social proof). NDW's unfair advantage: no other app fuses brain signals with voice biomarkers and lifestyle data into one score. | Added Oura/Whoop/Calm/Headspace to competitive-analysis.md with retention data. Created issue #464 for Daily Wellness Score (0-100 composite from sleep + EEG emotion + HRV + stress recovery). Added "daily composite score" and "share cards" to gaps table as Critical/High priority. |
| 2026-03-20 | ux-polish | Scores dashboard loading state was a bare spinner centered on an empty page, while dashboard, settings, session-history, and daily-brain-report all use Skeleton components. The scores page is the main health overview — its loading state should feel premium, not barren. Pages like Oura/Whoop show shimmer layouts that match the real content structure, preventing layout shift and giving users immediate spatial context. | Replaced bare spinner with full skeleton layout mirroring the real page: header, Energy Bank circle, 2x2 score card grid with icon+gauge placeholders, secondary row, and Today's Summary with 3 metric rows. Uses existing Skeleton component from shadcn/ui. |
| 2026-03-20 | medical-analytics | Voice depression screener (`voice_depression_screener.py`) output fields `phq9_score` (0-27) and `gad7_score` (0-21) used the exact PHQ-9/GAD-7 clinical scales and severity labels, but the scores are heuristic mappings from acoustic features with no clinical validation. A user or developer seeing `phq9_score: 18, severity: moderately_severe` would reasonably interpret it as a validated PHQ-9 result. The ADHD detector similarly classifies profiles into DSM-5 subtypes ("inattentive", "hyperactive", "combined"). Separate mental_health_questionnaire.py correctly implements actual PHQ-9/GAD-7 self-report — the voice screener borrowing the same scale creates dangerous confusion. | Strengthened `_DISCLAIMER` to explicitly state "NOT validated PHQ-9 or GAD-7 questionnaire results." Added `not_validated: true` flag and `scale_context` explanation to both `score_depression_risk()` and `score_anxiety_risk()` return dicts. Updated API route schemas, docstrings, and `/status` endpoint. All 18 tests pass. Filed issue #465 for full audit of all models using clinical scoring scales without validation evidence. |
| 2026-03-20 | eeg-brain | `preprocess()` in `eeg_processor.py` had zero NaN/inf handling. BrainFlow Bluetooth packet drops inject NaN samples into the EEG stream (1-5% dropout rate on native BT per CLAUDE.md). `scipy.signal.filtfilt` propagates a single NaN to the ENTIRE output array, making every band power, FAA value, emotion probability, and mental state index NaN. Once NaN enters the EMA state in `_predict_features()`, it stays NaN permanently until the classifier is reinitialized — a single dropped packet can poison the entire session. The LGBM paths had partial `np.isfinite` guards but the feature-based heuristic path (the actual live Muse 2 path) had none. | Added `_sanitize_nan()` function that linearly interpolates NaN/inf gaps before filtering (standard EEG approach for short BT drops). Called at the top of `preprocess()` so all downstream code is protected. Returns zeros for all-NaN signals (total disconnection). Fast path skips copy when signal is clean. All 22 IAF tests pass. |
| 2026-03-20 | ux-polish | Today page health metric cards (Sleep, Heart Rate, Steps, Nutrition) all rendered as identical flat gray boxes with "---" and "No data" in their empty state. No color identity, no visual hierarchy, no invitation to act — compared to the Discover page's Explore cards which have colored borders and emojis. The empty state is the FIRST thing new users see since no health data is synced initially, making it the worst possible first impression. | Added per-metric identity styling to HealthMetricCard empty state: colored left border accent (purple/rose/cyan/amber), subtle gradient tint background, emoji icon, and inviting CTA text ("Sync sleep data", "Connect Health to track", etc.) with a "Tap to get started" hint. Cards with actual data remain completely unchanged. |
| 2026-03-20 | human-emotion | Voice check-in "done" state showed bare emotion label + confidence number with zero explanation. Users saw e.g. "fear 72% confidence" with no context about what voice patterns led to that classification, what the emotion label means in this context, or that it reflects acoustic patterns rather than a clinical diagnosis. Per Scherer (2003), Juslin & Laukka (2003), and Schuller et al. (2018), each basic emotion has a distinct vocal acoustic profile (pitch, rate, energy, spectral tilt) that can be described in plain language. The result screen also lacked any wellness disclaimer despite presenting emotion classifications. | Added `EMOTION_CONTEXT` record mapping all 6 emotions to evidence-informed `voicePattern` descriptions and gentle `insight` text. Added a context card to the done state showing the insight + voice pattern. Added a non-clinical disclaimer line. No existing features removed. |
| 2026-03-20 | health-analysis-app | The Today page Wellness Gauge (0-100 score) -- the most prominent element on the home screen -- had no share mechanism. Oura/Whoop both allow sharing daily scores directly from the home screen. The EFS share card exists (1080x1920 Instagram Stories format) but is only accessible from the Emotional Fitness page. The Today page is where daily retention lives (users check score every morning) and where viral sharing should originate. Push notifications are well-designed (3 daily periods, personalized with last emotion, quiet hours) but referral/invite mechanics are completely absent. Community mood feature exists but is not prominently surfaced. | Added canvas-based share card generator (`shareWellnessScore`) to the Today page that renders a 1080x1080 PNG with gradient background, animated gauge arc, score, mood chip, AI insight text, and NDW branding. Added a "Share Score" button (purple pill, Share2 icon) below the Wellness Gauge that appears only when score > 0. Uses `navigator.share` with file support (native share sheet on mobile) with download fallback for desktop. |
| 2026-03-20 | ux-polish | Today page (most-seen screen) used 7 different font sizes (10px-24px), tight card padding (16px), and 10px grid gaps. Competitor apps like Oura and Bevel use generous whitespace with fewer font sizes and more breathing room. The dense layout made the app look "AI-generated" rather than premium. | Standardized to 4-level typography scale: 11px labels, 14px body, 16px subheadings, 28px scores (up from 24px). Increased bevelCard padding to 18px 20px, grid gaps from 10 to 14, and section margins from 14 to 20. All colors, animations, and functionality unchanged. |
| 2026-03-20 | medical-analytics | `get_feedback_stats()` in `user_feedback.py` double-counted state corrections in the accuracy denominator: `feedback_total = stats["corrections"] + (stats["total"] - stats["reports"])` expanded to `2*corrections + binary_feedback` instead of `corrections + binary_feedback`. A user with 8 corrections (6 correct) and 2 self-reports would see accuracy = 6/16 = 0.375 instead of the true 6/8 = 0.75. Additionally, `record_binary_feedback()` existed but had no API route, so thumbs-up/down feedback couldn't be submitted. And there was no `GET` endpoint to retrieve accuracy stats -- the computed `user_perceived_accuracy` was only returned inline in `personalization/status` for 6 hardcoded models. | Fixed accuracy formula to `feedback_total = stats["total"] - stats["reports"]`. Added `POST /feedback/binary` endpoint for thumbs-up/down feedback. Added `GET /feedback/stats/{user_id}` endpoint returning per-model accuracy with disclaimer that these reflect user-perceived accuracy, not clinical diagnostic accuracy. Added `BinaryFeedbackRequest` schema. Added 2 tests validating correct accuracy computation. All 29 tests pass. |
| 2026-03-20 | eeg-brain | `PersonalModelAdapter.adapt()` (SGDClassifier online learner in `online_learner.py`) was dead code. The SGD model was consulted during `predict_emotion()` to override the emotion label when confidence > 0.6, but was never updated by user corrections — `adapt()` was never called from any route. The `/feedback` endpoint recorded corrections in FeedbackCollector (k-NN pipeline) and PersonalModel (EEGNet fine-tuning) but skipped the SGD path entirely. Additionally, `adapt()` required raw EEG signals, but most web UI corrections are label-only (no raw signal attached). And `adapt()` required prior `calibrate()` — users who never ran the 3-step calibration could never benefit from SGD online learning even if they corrected hundreds of predictions. | Added `adapt_from_features()` method that accepts pre-extracted feature dicts (works with cached features from label-only corrections), auto-initializes the SGDClassifier on first correction without requiring prior calibration, and wired it into the `/feedback` endpoint. Now every correction feeds `partial_fit` so the SGD model improves incrementally. Added 5 tests validating auto-init, accumulation, prediction, unknown label rejection, and legacy signal path. All 22 IAF + 5 new tests pass. |
| 2026-03-20 | human-emotion | The mood-trends distribution chart only recognized 6 basic emotions (happy, sad, angry, fear, surprise, neutral) in its color/label maps, so the 10 nuanced emotions from voice check-in corrections (anxious, nervous, grateful, proud, lonely, hopeful, overwhelmed, peaceful, excited, frustrated) rendered as unlabeled gray bars. More critically, the `/api/brain/history/:userId` endpoint returned raw `dominantEmotion` from the DB without checking the `userCorrectedEmotion` field — meaning when a user corrected "neutral" to "anxious," the distribution still counted it as "neutral." Corrections were recorded in the database but never surfaced in any visualization. | Added all 10 nuanced emotions with distinct colors and labels to mood-trends.tsx EMOTION_COLORS/EMOTION_LABELS. Fixed brain history endpoint to prefer `userCorrectedEmotion` over `dominantEmotion` for EEG readings and `userCorrected` over `emotion` for voice readings. Distribution chart now reflects user-verified labels and shows nuanced emotions with proper visual identity. |
| 2026-03-20 | health-analysis-app | Comprehensive audit of 6 retention/growth gaps against Oura/Whoop/Calm/Headspace. Findings: (1) **Weekly summary notification** -- `NotificationPreferences` had `weekly_summary_enabled=True` and `NotificationRecord` supported type `"weekly_summary"`, but no `WeeklySummaryGenerator` class existed. Oura sends automated weekly recaps as a key re-engagement mechanism; NDW had the page (`weekly-brain-summary.tsx`) but no push notification to drive users back to it. (2) **No app store rating prompt** -- zero `requestReview` / `InAppReview` calls anywhere. (3) **No onboarding analytics** -- `ndw_onboarding_complete` is stored in localStorage but no tracking of completion rate, step abandonment, or path choice. (4) **No feature tour** -- 68 pages with no Joyride/Shepherd/coach-mark/what's-new system; the Discover page is static. (5) **Fixed notification timing** -- `NotificationScheduler` uses hardcoded hours (default 8am/9pm) with no learning from user engagement patterns. (6) **No deep linking** -- notification `route` fields exist for internal navigation, but no universal link / `appUrlOpen` handler / URL scheme registration for sharing specific insights. | Added `WeeklySummaryGenerator` class to `smart_notifications.py` that aggregates 7-day voice check-in history + health metrics into a concise notification with check-in count, avg stress/focus, dominant emotion, sleep score, and active-day streak. Added `GET /notifications/weekly-summary/{user_id}` endpoint, wired into test endpoint and schedule endpoint (next Sunday at morning_hour). Added 7 tests covering all branches. All 50 notification tests pass. Filed findings for remaining 5 gaps. |
| 2026-03-20 | privacy-security | The FastAPI ML backend (76+ endpoints) has **zero authentication** — no JWT validation, no API key, no middleware. All user-specific endpoints (sessions, feedback stats, weekly summaries, EEG analysis, biometric enrollment) accept any `user_id` from any caller. In production the ML backend runs on Railway as a separate service; if the URL leaks, all neurodata is exposed. Additionally, EEG biometric templates (`eeg_authenticator.py`) are stored as plain numpy arrays in process memory with no encryption, no persistence (lost on restart), no consent flow, no audit logging, and no replay-attack protection. GDPR Article 9 and the EU AI Act (2024) classify biometric data for identification as requiring explicit consent, encryption at rest, and enhanced access controls. CORS allows `*` when `CORS_ORIGINS=*` is set. | Created issue #466 (ML backend zero-auth on all 76+ endpoints — Critical) and issue #467 (biometric templates in-memory, unencrypted, no persistence, no consent — High). |
| 2026-03-20 | privacy-security | Two additional critical vulnerabilities found. (1) **Path traversal in EEG storage**: `save_eeg` endpoint constructs filesystem paths from unsanitized `user_id` and `session_id` — `Path("/base") / "../escape"` resolves outside the data directory. Combined with zero-auth (#466), any caller can write `.npz` files to arbitrary server paths. Same pattern in `ParquetWriter` and `training_data_stats`. (2) **Sensitive neurodata in unencrypted localStorage**: 47+ files store emotion classifications, health data, study participant codes, and auth tokens in plain `localStorage` — no TTL, no encryption, no XSS protection. `auth_token` in localStorage (not httpOnly cookie) is an OWASP anti-pattern. Privacy policy claims "never stored to disk" but derived emotion data IS persisted client-side indefinitely. No data retention/cleanup mechanism exists. | Created issue #468 (path traversal via unsanitized user_id/session_id in file paths — Critical) and issue #469 (neurodata in unencrypted localStorage with no expiry — High). |
| 2026-03-20 | neuroscience-papers | Neurofeedback protocol engine (`ml/neurofeedback/protocol_engine.py`) has **zero session safety guardrails**: no maximum duration (clinical standard 20-30 min, adverse effects documented beyond 45 min per Hammond 2011), no fatigue detection (alpha rebound, theta surge, score decline), no adverse-effect monitoring (headache in 15-20% per Hammond), no inter-session cooldown, no contraindication warnings (theta uptraining + epilepsy risk per Lubar & Shouse 1976). The `evaluate()` endpoint runs indefinitely. `neurofeedback_audio.py` cites Gruzelier (2014) but implements none of the dosing/safety constraints from that same paper. Alpha asymmetry protocol for mood regulation (evidence grade B) has no warning that unsupervised asymmetry training can cause emotional dysregulation. | Created issue #470 (neurofeedback session safety guardrails — duration limits, fatigue detection, adverse-effect monitoring). |
| 2026-03-20 | neuroscience-papers | Neurofeedback system has **no cross-session progress tracking or dosing guidance**. Clinical neurofeedback requires 20-40 sessions for neuroplastic effects (Gruzelier 2014, Enriquez-Geppert 2019: 25-30 sessions for ADHD), optimal dosing 2-3x/week with >=1 day spacing, and gaps >2 weeks cause skill decay (Hammond 2011). The system stores per-session stats in a deque(maxlen=60) that resets each session and trajectory JSON files for RL fine-tuning, but: (1) no resting baseline tracking across sessions (the primary outcome measure), (2) no learning curve data (session-over-session score improvement), (3) no dosing compliance alerts (over-training same-day or under-training 2-week gap), (4) no transfer assessment (whether neurofeedback effects appear in non-training EEG sessions), (5) no protocol completion milestones (users have no idea if training is working or when to stop). | Created issue #471 (cross-session neurofeedback progress tracking and dosing guidance). |
| 2026-03-20 | neuroscience-papers | Dream detector claims 97.20% accuracy but `ml/training/train_dream.py` reveals this was trained on **synthetic EEG** from the app's own `simulate_eeg()` function, not real polysomnographic data. The `generate_training_data()` function creates ideal spectral profiles that the model trivially separates — circular validation. Real REM detection accuracy on consumer-grade EEG is 75-85% (Sleep-EDF benchmarks). A `load_real_data()` function exists (loads Sleep-EDF via `load_rem_detection()`) but the default training path uses the `--simulated` flag. The inflated number creates false confidence downstream in lucid dream detection, dream journal, and dream analysis features. No published study validates REM detection from Muse 2's AF7/AF8/TP9/TP10 electrode placement specifically. | Created issue #472 (dream detector synthetic data validation — retrain on real Sleep-EDF with cross-subject splits). |
| 2026-03-20 | eeg-foundation-models | **EEGPT (NeurIPS 2024)**: 10M-param pretrained transformer supports any electrode count (1-138) via electrode-wise modeling. Pretrained on 37.5M samples. **LaBraM (ICLR 2024 Spotlight)**: 5.8M-369M params, pretrained on 2,500h EEG, outperforms baselines on SEED-V emotion. **Key for NDW**: EEGPT handles 4-channel Muse natively — no channel-count adaptation needed. Could replace feature-based heuristics with fine-tuned foundation model. EEG-FM-Bench (2025) shows EEGPT and LaBraM have emergent emotion embedding structure other models lack. Repos: github.com/BINE022/EEGPT, github.com/935963004/LaBraM | Research only — evaluate EEGPT fine-tuning on Muse 4-ch data in future session. |
| 2026-03-20 | eeg-muse-validation | **2024 paper (Computers in Biology & Medicine)**: Muse headband achieves G-Mean only 1-2% lower than full 32-channel DEAP setup for emotion detection. Gamma band most effective for valence. Optimal window: 5-30 seconds. Conclusion: Muse is "viable, lower-cost alternative" for everyday emotion detection. **This validates our entire approach.** Source: sciencedirect.com/science/article/abs/pii/S0010482524015488 | Key validation — reference in README and investor materials. |
| 2026-03-20 | voice-emotion | **emotion2vec (ACL 2024)**: First universal speech emotion representation model. SOTA on IEMOCAP with just linear probe. emotion2vec+ on HuggingFace: 9-class models in seed/base/large. Integrated into FunASR for real-time streaming. **Emotion2Vec-S** (2025): smaller variant for on-device. **SpeechBrain**: plug-and-play wav2vec2 emotion model on HuggingFace (speechbrain/emotion-recognition-wav2vec2-IEMOCAP). **For NDW**: emotion2vec seed model could replace current voice analysis for on-device APK inference. | Evaluate emotion2vec seed model size and latency for mobile deployment. |
| 2026-03-20 | food-mood | **Gut-brain axis research (2024-2025)**: 90% of serotonin receptors in gut. Mediterranean diet protects against depression (SMILES trial). Key nutrients: omega-3, B-vitamins, magnesium, vitamin D. **Apps**: NourishMate (AI food-mood pattern analysis, 1-2 weeks for patterns), ZOE (real-time glucose + mood + microbiome). **For NDW**: Can correlate EEG-derived emotional states with food logs — objective brain-based validation instead of self-report. 5-30 sec EEG windows could capture pre/post-meal emotional shifts. | Add food-mood correlation engine to nutrition page in future sprint. |
| 2026-03-20 | fitness-eeg | **Exercise effects on EEG**: Most common = increased alpha post-exercise (systematic review). Brief intense resistance training affects neural oscillations. **Muse S Athena (March 2025)**: First consumer EEG + fNIRS device. **Muse app**: "Alpha Peak Score" tracks cognitive efficiency, "Brain Recharge Score" = alpha-based mental recovery metric. **Enso AI Brain Coach**: transformer-based FBM trained on 1B+ minutes of EEG data. **For NDW**: Alpha power rebound post-exercise as biomarker for "brain recovery score" — differentiating feature. | Design brain recovery score based on alpha rebound timing/magnitude. |
| 2026-03-20 | competitor-ux | **Oura (Oct 2025 redesign)**: 5 tabs → 3 tabs (Today/Vitals/My Health). Time-aware dashboard changes content by time of day. Personal baselines, not population norms. Color as immediate signal. **Oura Advisor**: LLM chatbot with adaptive tone, generates charts in conversation. **Whoop 5.0**: Interconnected metrics (sleep→recovery→strain). Coach powered by OpenAI with "Daily Outlook" morning summary. **Muse**: 3-state simplification (calm/neutral/active), audio biofeedback (weather sounds), Muse Score unique visual reward. **Calm**: ring progress on home, minimal data. **Headspace**: time-of-day content, streak gamification. **Samsung Health (CES 2026)**: passive voice + gait analysis for brain health. | Apply: time-aware Today page, personal baselines, interconnected metrics, morning briefing pattern. |
| 2026-03-20 | competitor-ux | **AI wellness apps shipping now**: Whoop Coach (OpenAI), Oura Advisor (LLM), Muse Enso (transformer FBM on 1B min EEG), Amazon One Medical (Bedrock), ChatGPT Health (medical records). **Key pattern**: conversational interface dominates — chat over dashboards. AI anchored to YOUR data. Context-aware (time, activity, trends). Plain language translation of complex biometrics. AI positioned as companion/coach. **For NDW**: AI Companion should be context-aware (use current EEG state + recent voice emotions + health data in every response). | Enhance AI Companion with real-time biometric context injection. |
| 2026-03-20 | shipped-eeg-emotion | **Emotiv**: Most established commercial EEG emotion player. EPOC X (14-ch) has 6 performance metrics (stress, engagement, relaxation, excitement, interest, focus). MN8 Earbuds (2-ch) for workplace — focus, stress, productivity. **Key caveat**: "Cannot read thoughts or feelings" — hedged language. **NeuroSky MindWave**: Poor accuracy, "poor at recognising meditative/attentive states." **Hume AI**: Real-time voice emotion via WebSocket + eLLM (empathic LLM) that modulates response based on detected emotions. Full SDK (React, Python, Swift). **For NDW**: Combine EEG emotion (Emotiv-style metrics) + voice emotion (Hume-style streaming) = unique multimodal advantage no competitor has. | Position NDW as first multimodal (EEG + voice) consumer emotion platform. |
| 2026-03-20 | open-source-tools | **TorchEEG/TorchEEGEMO**: Comprehensive PyTorch library for EEG emotion — supports DEAP, SEED, DREAMER out of box. pip install torcheeg. **MuseLSL**: Muse streaming via LSL over BLE (bleak). **For NDW ML pipeline**: TorchEEG could standardize our training pipeline and provide plug-and-play model architectures (CNNs, GCNs, Transformers) instead of custom training scripts. | Evaluate TorchEEG for standardizing training pipeline. |
| 2026-03-20 | edge-models | **HiSTN (2025)**: ~1K parameter EEG emotion model achieves 96.82% F1 (valence), 95.62% F1 (arousal) on 5-class DREAMER. Hierarchical graph network handles varying electrode topologies. At ~1K params, trivially deployable on any mobile device. **ONNX Runtime Web + WebGPU**: 20x speedup over CPU, ~15ms inference on mid-range Android. **For NDW**: Export EEGNet or HiSTN to ONNX, run via ONNX Runtime Mobile. Entire EEG pipeline could run in-browser via Capacitor WebView. Source: sciencedirect.com/science/article/abs/pii/S0925231225020624 | Evaluate HiSTN vs EEGNet for on-device ONNX deployment. |
| 2026-03-20 | multimodal-fusion | **EmotionTFN (2025)**: Multi-scale temporal fusion (0.5-2s, 2-10s, 10-60s) fusing EEG+PPG+GSR+audio achieves 94.2% accuracy, sub-200ms latency on IoT. **Cross-modal attention beats concatenation by ~12%** (81.14% vs 71.02%). **For NDW**: Fuse Muse EEG + phone mic (voice) + camera PPG via cross-modal attention, not feature concatenation. The multi-scale temporal approach matches our existing 4-sec epoch + EMA smoothing. | Implement cross-modal attention fusion for EEG + voice in future sprint. |
| 2026-03-20 | transfer-learning | **Cross-headset teacher-student distillation (arXiv 2503.05349, March 2025)**: Train teacher on 32-ch DEAP, distill to student using only 4 common electrode positions. **4-Channel CNN cross-dataset (MDPI 2025)**: 97.98% on SEED 3-class with just 4 EEG channels, demonstrating cross-dataset generalization. **KD-DANN**: Transformer teacher distilled to Bi-LSTM student, small enough for mobile. **For NDW**: Teacher-student pipeline from DEAP/DREAMER 32-ch → Muse 4-ch is validated and practical. | Design teacher-student distillation pipeline for Muse-specific model. |
| 2026-03-20 | personalization | **BrainUICL (ICLR 2025)**: Unsupervised continual learning — pretrained EEG model adapts to new subjects one by one without forgetting. No user labeling required. **TCPL (Frontiers 2025)**: Few-shot cross-subject adaptation with 2-3 min calibration data. **Continual fine-tuning (Feb 2025)**: 61-subject longitudinal study shows successive fine-tuning improves session-over-session without recalibration. **On-device EEGNet learning (2024)**: EEGNet adapted for on-device learning on wearable hardware. **For NDW**: BrainUICL-style unsupervised adaptation = model improves automatically as user generates data. No calibration friction. Repo: github.com/xiaobaben/BrainUICL | Evaluate BrainUICL for automatic personalization without user labeling. |
| 2026-03-20 | sleep-mood-eeg | **Sleep-emotion bidirectional link (SLEEP journal 2025)**: Emotion features improve sleep stage classification via deep learning on EEG. **Prefrontal alpha biomarker (Nature 2025)**: High alpha (11-13 Hz) from 3-channel portable prefrontal EEG = gender-sensitive biomarker for emotional disorders. **Portable EEG depression detection (PMC 2025)**: Reduced alpha + enhanced beta + prefrontal alpha asymmetry = depression markers. Peak publications 2024 (99 articles). **For NDW**: Alpha asymmetry from AF7/AF8 = simple daily mood biomarker. Sleep with Muse → wake with mood prediction. | Add daily alpha-asymmetry mood biomarker to Today page. |
| 2026-03-20 | neurofeedback-design | **Session parameters (Frontiers 2025 meta-analysis)**: 20-50 min sessions, 8-20 sessions for neuroplastic effects. **Spaced practice beats massed (2025 RCT)**: 3-day intervals outperform 2-day for long-term retention. **Don't encourage daily sessions** — every 2-3 days is optimal. **Gamification**: Variable reward schedules > fixed; 77% of users quit after 1st session; personalized rewards increase return by 40%. **For NDW**: Target 20-min sessions, every 2-3 days (not daily streaks for neurofeedback). First-session experience is critical — 77% churn rate. | Redesign neurofeedback scheduling: 2-3 day spacing, NOT daily streaks. |
| 2026-03-20 | voice-on-device | **Wav2Small (audEERING 2024)**: 72K params, 120KB ONNX model. Predicts arousal/dominance/valence. Distilled from wav2vec2. Smallest viable voice emotion model found. **SenseVoice-Small (Alibaba)**: 70ms for 10s audio, ASR + emotion in one model, supports Android/iOS/Raspberry Pi, GGML quantization. **emotion2vec NOT ONNX-exportable** — open issue, too large for mobile. **For NDW APK**: Wav2Small (120KB ONNX) is the clear winner for on-device voice emotion. SenseVoice if we also need transcription. | Replace server-dependent voice analysis with Wav2Small ONNX on-device. |
| 2026-03-20 | voice-biomarkers | **Kintsugi open-sourced Feb 2026** — clinically validated voice biomarker models for depression/anxiety. 71.3% sensitivity for PHQ-9 >= 10. All models + methodology on GitHub (KintsugiMindfulWellness). Company closed but research is public. **Sonde Health**: Integrated into Qualcomm Snapdragon S7+ for always-on earbud voice monitoring. **Key vocal features**: jitter, shimmer, pause times, speech rate, spectral tilt. Depression = longer pauses + monotone. AUC 0.93 for MDD detection. **For NDW**: Grab Kintsugi's open-source models — free, clinically validated. | Download and evaluate Kintsugi open-source voice biomarker models. |
| 2026-03-20 | circadian-emotion | **Evening chronotypes have 2x mood disorder risk** (Nature 2025). Alpha/beta power varies significantly by circadian phase — morning vs evening types show different EEG patterns at different times. **Must adjust EEG baselines by time-of-day.** Collect chronotype via MEQ questionnaire, apply correction factors. At minimum distinguish "optimal" vs "non-optimal" time windows per user. **For NDW**: Add chronotype questionnaire to onboarding, adjust emotion baselines by time-of-day. Source: nature.com/articles/s41598-025-00893-8 | Add chronotype assessment to onboarding flow; time-of-day baseline adjustment. |
| 2026-03-20 | food-mood-data | **Minimum data for food-mood patterns**: 2-4 weeks for individual patterns, 3 months for robust dietary correlations. Mediterranean diet → 32-45% reduction in depressive symptoms. Fruits, dark leafy greens, dairy show direct associations with psychological distress even in cross-sectional analysis. **Bearable app**: 75% of users report feeling more in control; richest correlation engine among consumer apps. **For NDW**: Set user expectations — "patterns emerge in 2 weeks, detailed insights in 6 weeks." | Add food-mood correlation timeline messaging to nutrition page. |
| 2026-03-20 | neuroscience-papers | Creativity model (99.18%, 850 samples, 4 classes) is a textbook overfit: ~212 samples per class with the feature dimensionality used. CLAUDE.md flags this but no remediation exists. The cited studies (Kounios & Beeman 2014, Fink & Benedek 2014) used 64-128 channel gel EEG; alpha synchronization across frontal regions requires spatial resolution 4-channel Muse 2 cannot provide. Flow state model (62.86%, 4 classes) is only ~38 points above chance (25%). Katahira et al. (2018) achieved ~70-75% with 32 channels in a controlled Tetris paradigm. The transient hypofrontality theory (Dietrich 2004) has been challenged by Weber et al. (2024) showing high individual variability. The quadratic theta model in the code is sound but requires per-user calibration that is implemented (`calibrate()`) but never enforced. | Created issue #473 (creativity overfit + flow marginal accuracy — evidence-based validation needed). |
| 2026-03-20 | data-augmentation | **Diffusion models (DDPM/DDIM) are best for EEG augmentation** — surpass GANs in stability and diversity. DDPM-generated synthetic EEG achieves >95% classification accuracy. Geometric transforms (rotation, scaling) DO NOT work for EEG — disrupt temporal/spectral structure. **Best strategy for limited subjects**: (1) diffusion model synthetic data + (2) domain adaptation for cross-subject + (3) few-shot fine-tuning on new users. **DFF-Net (2023)**: domain adaptation + few-shot achieves 93.37% on SEED cross-subject. **Contrastive learning (Nature 2025)**: aligns EEG representations across subjects without large labeled datasets. | Implement DDPM augmentation for Muse training data; evaluate DFF-Net for cross-subject transfer. |
| 2026-03-20 | neuro-privacy | **EU AI Act (Aug 2, 2026)**: EEG emotion detection becomes HIGH-RISK AI system requiring conformity assessment, risk management, data governance. Already BANNED in workplaces/schools (Feb 2, 2025). **Chile**: First constitutional neurorights, first court ruling (Emotiv violated neuroprivacy). **US states**: Colorado (Aug 2024), California (Jan 2025) classify neural data as sensitive PII. Montana, Connecticut also. **UNESCO (Nov 2025)**: First global neurotechnology ethics framework — neural data = special category sensitive data, bans on non-therapeutic child neurotech. **Industry audit**: 96.7% of neurotech companies reserve right to transfer brain data to third parties, <20% mention encryption. **For NDW**: Must comply with EU AI Act by Aug 2026. Add explicit consent for neural data. Never sell/share brain data. Encrypt at rest. | Critical: audit NDW against EU AI Act high-risk requirements before Aug 2026. |
| 2026-03-20 | artifact-removal | **ICA doesn't work with 4 channels** — use alternatives. **Best for Muse 2**: (1) Wavelet denoising (FF-EWT or DWT) per-channel, (2) Adaptive filtering with Savitzky-Golay reference for blink removal (no EOG reference needed), (3) Optional LSTM autoencoder (CLEnet 2025) for final cleanup. **AnEEG (Nature 2024)**: DL model outperforms wavelet, works per-channel. **Detection-guided framework (Feb 2026)**: Only corrects contaminated segments, preserves clean data. **Recommended pipeline**: blink detection via template → wavelet denoise per-channel → adaptive filter for residual ocular → optional CNN/LSTM cleanup. | Replace current artifact rejection (simple threshold) with wavelet + adaptive filter pipeline. |
| 2026-03-20 | explainable-ai | **Best XAI for consumer EEG**: (1) Topographic heatmap (which of 4 electrodes contributed most), (2) Band power bar chart (alpha/beta/theta contribution), (3) Temporal highlight (salient moments), (4) Plain-language narrative ("Your left frontal alpha is high = relaxed positive state"). **Grad-CAM** most widely adopted for EEG models. **Self-attention prototypes (Nature 2024)**: intrinsically explainable — shows representative EEG pattern user's state matches. **For NDW**: Add "Why this emotion?" card to voice check-in results showing band power contribution + plain-language explanation. | Design "Why this emotion?" explainability card for Today page and voice check-in results. |
| 2026-03-20 | digital-biomarkers | **Brain age is most validated consumer EEG biomarker** — 5,200+ subjects study with Muse S (Paris-Saclay). **Attention via theta/beta ratio**: well-established, FDA-cleared for ADHD (NEBA system). **Sleep staging from consumer EEG**: validated in 43-study meta-analysis (npj 2025). **CRITICAL**: Muse's "calm" meditation score does NOT correlate with validated mindfulness measures (PubMed). **$35 4-channel EEG system (2025)**: Self-supervised contrastive learning achieves 60.2% emotion accuracy — 20.4% improvement over conventional features. **For NDW**: Brain age as a premium feature. Don't claim meditation depth is validated. | Add brain age estimation as premium biomarker. Caveat meditation depth claims. |
| 2026-03-20 | eeg-music | **40 Hz gamma entrainment**: Promising for cognitive enhancement and Alzheimer's. Stanford meta-analysis: gamma beats improved attention in ADHD adults. **FRENZ Brainband (CES 2025)**: Real-time EEG drives personalized frequency music and binaural beats. **Brain.fm + Muse**: Neural Phase Locking verified by EEG. **LLM-powered music therapy (Jan 2026)**: Lightweight EEG processing → LLM reasoning agent → physiologically-informed music recommendations. Democratizes music therapy without experts. **EEG Emotion Copilot (2025)**: Optimizes lightweight LLMs for EEG interpretation. **For NDW**: Quick Listen Spotify section could be upgraded to EEG-adaptive music recommendations. | Design EEG-adaptive music recommendation feature using current Spotify integration. |
| 2026-03-20 | health-integration | **Apple HealthKit**: No EEG data type. CAN write Mindful Minutes + HKStateOfMind (38 emotion labels, iOS 18+). Map neurofeedback sessions → Mindful Minutes, map voice emotions → State of Mind. **Google Health Connect**: No mood/EEG types. CAN write MindfulnessSession. New FHIR Medical Records API (March 2025) could carry EEG DiagnosticReport. **Capacitor plugins**: @capgo/capacitor-health is best unified option. **FHIR for EEG**: No dedicated type — use Observation + DiagnosticReport + LOINC 11524-6. Raw EEG stored as EDF format. | Integrate Mindful Minutes writes via @capgo/capacitor-health. Map voice emotions to HKStateOfMind on iOS. |
| 2026-03-20 | eeg-form-factors | **EEG earbuds shipping**: Emotiv MN8 (2-ch, $299), IDUN Guardian 4 (in-ear, $399, open Python SDK), Neurable MW75 (ear-pad, $499-$699). **Smallest EEG device**: Awear behind-the-ear clip ($195, TechCrunch Disrupt 2025 winner, Stanford testing). **Muse S Athena**: First consumer EEG + fNIRS combo ($349+). **Best open API**: BrainBit (free SDK, all platforms), IDUN (Python SDK + Public API). **No EEG ring exists** — physics of finger placement prevents viable signal. **For NDW**: Plan multi-device support. BrainBit has broadest free SDK. | Add BrainBit and IDUN Guardian as future supported devices alongside Muse. |
| 2026-03-20 | monetization | **Hardware + subscription dominates**: Muse ($249-349 hardware + $12.99/mo premium, $5.8M annual revenue). Emotiv (tiered: $299-$2299 + paid SDK license). Myndlift ($199 kit + $29-$150/mo with coaching). **Trend**: Lower hardware cost with subscription lock-in. Awear includes lifetime subscription at $195. **B2B/Enterprise** = highest revenue per customer (workplace wellness, clinical, research licensing). **Free SDK approach** (BrainBit) monetizes hardware + B2B deals. **For NDW**: Consider freemium model — basic EEG wellness free, premium features (brain age, AI coaching, advanced analytics) behind paywall. | Design freemium tier structure for NDW launch. |
| 2026-03-20 | fda-regulatory | **FDA 2026 General Wellness Guidance (Jan 6, 2026)**: Two-factor test — (1) general wellness use only + (2) low risk = no FDA regulation. **Safe claims**: "helps relax," "tracks focus," "supports meditation," "promotes sleep habits." **Triggers FDA**: "diagnoses," "treats," "clinical improvement," referencing ADHD/depression/anxiety, displaying PHQ-9/GAD-7, using "therapy"/"treatment." **Classification depends on MARKETING, not technology.** Same EEG hardware can be wellness product or Class II medical device based on claims alone. **For NDW**: Remove or disclaim all PHQ-9/GAD-7 references. Use "supports wellness" language only. Never claim to diagnose or treat. | Audit all NDW marketing copy, API docs, and UI text for FDA-triggering language. |
| 2026-03-20 | fine-grained-emotion | **EEGEmotions-27 (IEEE 2025)**: 27 fine-grained emotion labels, 88 participants, deep learning achieves 62.24% across 27 classes. "High-granularity annotation IS decodable." **FACED (2023)**: 9-class, 123 subjects, best 59.3% for 9-class, 88.1% for 3-class. **Reality for 4-ch Muse**: Target 3-5 classes max from EEG alone. Compound emotions (anxiety, gratitude, loneliness) unsolved from EEG-only. **V-A-D dimensional approach gaining over discrete labels** — matches our current architecture. | Stick with V-A-D dimensions + configurable emotion mapping. Don't claim fine-grained from EEG alone. |
| 2026-03-20 | meditation-validation | **JMIR 2025 meta-analysis (16 RCTs, 763 participants): "Consumer-grade devices enabling meditation depth modulation is NOT currently supported."** Effects may be "neurosuggestion" (placebo). BUT: devices ARE sensitive to attention lapses, and home practice metrics explain ~30% variance in trait mindfulness. **Best validated markers**: Alpha power trends (Muse is reliable here) + attention lapse detection (validated). Frontal midline theta (Fmz) is strongest evidence but Muse has AF7/AF8 (lateral), not Fmz. **For NDW**: Track alpha trends + lapse events. Do NOT claim "meditation depth." Voice journaling before/after adds subjective grounding. | Replace "meditation depth" claims with "attention quality" and alpha trends. |
| 2026-03-20 | biofeedback-ux | **Data overwhelm causes anxiety** (npj Digital Medicine 2025). **Visual > numerical** for engagement. **Progressive disclosure**: summary first, raw data on drill-down. **CHI 2025**: Setup time and intrusiveness predict dropout MORE than feature count — 77% quit after first session. **Best visualizations**: ambient (breathing circles, color gradients) > analytical (bar charts) > raw (waveforms). **Customization matters** — users want to choose which data to see. **For NDW**: Default to ambient visuals, use hedging language ("suggests relaxation" not "you are calm"), limit notifications, let users customize dashboard. | Redesign biofeedback pages: ambient visuals default, progressive disclosure, reduce notification frequency. |
| 2026-03-20 | longitudinal-personalization | **Personalized models consistently outperform generalized** (Taylor & Francis 2025). Few-shot fine-tuning with 5-10 labeled samples recovers substantial cross-subject drop. **35-40 sessions** needed for structural prefrontal changes (neurofeedback). **No published study shows consumer EEG accuracy improvement curves over weeks/months in controlled setting** — evidence is fragmented. **Voice may be faster path to personalization** (larger signal, less hardware-dependent). **For NDW**: Day 1 calibration essential (5-10 min). Voice personalization as quick win. EEG personalization as longer-term investment. | Implement Day 1 calibration flow + voice-first personalization. |
| 2026-03-20 | affective-ethics | **EU AI Act**: Emotion AI BANNED in workplaces/education (Feb 2, 2025). Full high-risk rules Aug 2, 2026. **Frontiers 2026**: Significant accuracy disparities across age, gender, ethnicity — training data lacks diversity. Dominant models from WEIRD populations. **ACM FAccT 2025**: Regulatory gaps in US emotion AI. **ACII 2025 theme**: "Socially Responsible Affective Computing." **For NDW**: Never present uncertain readings as definitive. Disclose cultural bias limitations. Continuous consent (not just EULA). Never market for workplace/education in EU. | Add demographic bias disclosure + ongoing consent mechanism. |
| 2026-03-20 | voice-eeg-fusion | **Multimodal consistently outperforms unimodal**: EmotionTFN 94.2% (5-modal), GNN 91.25% (EEG+audio+visual). **Attention-weighted fusion is dominant** — adapts per user (if EEG noisy, lean on voice). **Expected boost from adding 4-ch EEG to voice: 3-8pp.** **Real value**: capturing felt vs expressed emotion divergence ("I'm fine" but EEG shows high arousal). **Best strategy**: attention-weighted late fusion, voice as primary modality, EEG as complementary arousal/valence. **EAV dataset** (42 subjects, conversational) directly relevant for training. | Implement attention-weighted late fusion: voice primary, EEG secondary. Train on EAV dataset. |
| 2026-03-20 | eeg-llm | **Thought2Text (NAACL 2025)**: Working brain-to-text pipeline — EEG encoder → LLM fine-tuned on image+text → generates text from EEG embeddings. **EEG Emotion Copilot (Neural Networks 2025)**: 0.5B param LLM runs locally, takes EEG, performs emotion recognition + generates reports. No cloud needed. **For NDW**: Feed Muse EEG stream into lightweight LLM for real-time emotion labeling or AI companion context. The 0.5B local model architecture is realistic for mobile. | Explore feeding real-time EEG context to AI Companion for brain-aware responses. |
| 2026-03-20 | dream-tech | **Prophetic Halo ($1,500-2,000)**: Transcranial focused ultrasound guided by ML on EEG+fMRI for lucid dream induction. **REMspace LucidMe PRO**: First deliveries March 2026, claims lucid dream induction during REM. **Muse S**: Can detect REM onset but cannot trigger cues without external hardware. No official SDK — relies on Mind Monitor OSC. **For NDW**: Dream detection feature is valid (REM detection from Muse). Lucid dream induction requires separate hardware. | Dream journal + REM detection is feasible. Lucid dream induction requires hardware partnership. |
| 2026-03-20 | exercise-eeg | **Movement artifact is the core problem** with consumer EEG during exercise. **IMU-enhanced artifact removal (arXiv Sept 2025)**: pairs accelerometer data with EEG to subtract motion noise. **WalkingWizard headset**: 87% SSVEP accuracy across walking speeds (purpose-built, not Muse). **Muse degrades significantly with movement**. Only gross state changes (high arousal vs calm) detectable during motion. Yoga/slow movement feasible, running not. **For NDW**: Exercise EEG limited to post-exercise alpha rebound (brain recovery score) rather than during-exercise tracking. | Brain recovery score = post-exercise alpha rebound timing. Don't claim during-exercise accuracy. |
| 2026-03-20 | microdosing-eeg | **Measurable EEG changes from psilocybin**: double-blind study found 0.5g significantly decreased theta-band power (4-8 Hz) in eyes-closed resting. Veterans study: decreased delta+theta in frontal/temporal. BUT meta-analysis found NO cognitive benefit and DECREASED cognitive control. **10M US adults microdosed in 2025** (RAND). Legal in Oregon, Colorado, clinical trials. **For NDW**: Could track theta-band power across microdosing as personal biomarker journal. Firmly "quantified self" — no medical claims. Theta power is the most measurable signature with Muse. | Potential future feature: theta-band tracking journal for quantified self users. No medical claims. |
| 2026-03-20 | social-eeg | **Brain synchrony is real**: dynamic interbrain network patterns during social interaction confirmed (Communications Biology 2026). **Music therapy is the killer app** — 26/32 hyperscanning studies used EEG. Therapist-patient and parent-child neural sync measurable. **Consumer hyperscanning demonstrated** with 6-15 simultaneous participants. **Two Muse headbands** could show real-time inter-brain synchrony for couples meditation. **No app currently does this with Muse.** **For NDW**: Couples/partner meditation with brain sync visualization = unique differentiator no competitor has. | Design couples meditation feature with dual-Muse brain synchrony visualization. |
| 2026-03-20 | closed-loop-stim | **Flow Neuroscience FL-100**: FDA-cleared for at-home tDCS treatment of depression, launching Q2 2026. First consumer brain stimulation with regulatory approval for unsupervised home use. **neuroConn LOOP-IT**: research-grade closed-loop tDCS+EEG, <100µs latency. **Muse is read-only** — no stimulation. Could guide separate tDCS sessions ("your alpha is low, consider a session"). **For NDW**: Partner with Flow Neuroscience or similar for guided stimulation protocols using Muse EEG as feedback signal. | Explore partnership with Flow Neuroscience for EEG-guided tDCS integration. |
| 2026-03-20 | onnx-android | **WebGPU does NOT work in Capacitor Android WebView** (open issue #8044). Only WASM (CPU) and WebGL (maintenance mode, unstable on mobile) are available. **WASM is the safe choice** for small models. WebGL crashes and freezes on mobile. Memory tighter in WebView vs Chrome. For models <50MB, WASM is fine. Larger models → native Capacitor plugin. **For NDW**: Our 344KB voice ONNX models will run fine via WASM. EEGNet (~85KB) also safe. Don't use WebGL on mobile. | Use WASM backend for all ONNX inference on mobile. No WebGPU/WebGL. |
| 2026-03-20 | signal-quality | **Muse's green/yellow/red indicators can show green even with poor signal** (research confirmed). Better quality metrics: **spectral flatness** (flat=noise, peaked=real), **alpha reactivity** (eyes-open vs eyes-closed), **amplitude variance**. Multi-pin electrodes outperform Muse's flat gold for hairy areas. **Best UX**: per-electrode real-time feedback + "close eyes for 5s" alpha self-test. Nature 2026 recommends spectral patterns over impedance checks. | Add alpha-reactivity quality test + per-electrode status to connection flow. |
| 2026-03-20 | haptic-biofeedback | **Apple Watch haptic biofeedback**: Vibration patterns slower than real-time HR → increased parasympathetic activity and relaxation (ACM 2025). BUT no significant sleep improvement. **Capacitor Haptics API is limited** — only discrete events (impact, vibrate), not continuous waveforms. iOS Taptic Engine richer than Android. Need native plugin for sophisticated rhythmic patterns. **Evidence still early-stage** — supplementary modality, not core therapeutic. | Optional haptic feedback for meditation sessions. Don't position as core feature. |
| 2026-03-20 | youth-eeg | **Consumer EEG physically safe for children** but efficacy questionable — benefits may be "neurosuggestion" (placebo). ADHD neurofeedback: small-medium improvements, ~40% placebo. **Regulatory gap**: DTC devices escape oversight, amplified concern for minors. **Developing brains more susceptible** — cuts both ways. No major app targets under-18 with EEG emotion. **If pursuing**: IRB-level informed consent + parental controls essential. | Do NOT target under-18 without clinical validation and parental consent framework. |
| 2026-03-20 | neurotech-market | **BCI funding exceeded $803M in 2025**, 2026 tracking $1.3B. Market: $15.3B (2024) → $52.9B by 2034. **Consumer wearables ~$300M by 2026** at 15% CAGR. Gaming/entertainment fastest segment (13.5% CAGR). **Key rounds**: Neuralink $650M, Synchron $75M, Kernel $100M+. China BCI market ~$530M. Consumer wellness is the growth wedge but actual adoption still small vs clinical/research. | NDW positioning: consumer wellness with research credibility. Market is growing fast. |
| 2026-03-20 | notification-strategy | **Sweet spot: 1-3 notifications/day, batched** (reduces stress vs constant). First 90 days: 1+ push notification → 3x higher retention. Mental health apps have brutal **4% retention at 15 days** (vs 21% average). **ML-driven send-time optimization** beats fixed schedules. **Anxiety risk**: excessive check-in prompts increase rumination (The Conversation 2025). **Best practice**: default minimal (1x/day), let users opt up, frame as invitations not obligations, never guilt-trip missed sessions. | Default 1x/day morning notification. Let users choose frequency. No guilt language. |
| 2026-03-21 | supabase-scale | **TimescaleDB extension available on Supabase** (Apache 2 edition only). Auto-partitions by time. Advanced features (continuous aggregates, compression policies) need self-hosting. **For EEG at scale**: offload raw EEG to Supabase Storage (object), keep only aggregated metrics in Postgres. Range partition on timestamp. **HIPAA path exists** via Supabase healthcare solution. No public case study of 10K+ EEG users on Supabase yet. | Use TimescaleDB for EEG readings table. Raw EEG → Storage, aggregates → Postgres. |
| 2026-03-21 | app-store | **Google Play (Jan 2026)**: Mandatory Health Apps Declaration. First paragraph must include "not a medical device" disclaimer or = rejection. Must migrate to Organization Account. **Apple**: Tightened privacy labels — must declare all collected data + AI/ML explanation. **FDA (Jan 2026)**: New guidance clarifies wellness vs clinical decision support. "General wellness" = lower scrutiny but narrow line. | Verify Play Store listing has mandatory disclaimer. Check Apple privacy labels cover all data types. |
| 2026-03-21 | onboarding | **3-5 screens, under 60 seconds** to first value. **Just-in-time permissions** (not batch) increase grant rates 30-40%. Request Bluetooth when user taps "Connect Device," not at launch. **Muse pattern**: headband on → connect → first session immediately. Minutes to first EEG reading. **Enso AI coaching** is a Premium upsell post-onboarding. | Redesign onboarding: 4 screens max, just-in-time permissions, first value in 60s. |
| 2026-03-21 | analytics | **PostHog has first-party Capacitor plugin** with consent management, cookieless tracking, session replay, feature flags, A/B testing, surveys. BAA for HIPAA (Boost+ plan). **Key metrics**: DAU/MAU (20%+ good), session length, "sessions completed" and "EEG connection success rate" as leading indicators. **Alternatives**: Mixpanel (BAA), Amplitude (warehouse-native HIPAA). PostHog = best all-in-one for Capacitor. | Install PostHog Capacitor plugin. Track: connection success rate, sessions completed, D1/D7/D30 retention. |
| 2026-03-21 | offline-first | **PowerSync + Supabase** = recommended offline-first stack. Syncs in-app SQLite with Supabase Postgres, handles conflict resolution. **Alternative**: RxDB with Supabase Replication Plugin. **For health/EEG data**: last-write-wins sufficient (append-only readings). **Oura/Whoop pattern**: record locally first, queue sync, batch upload on reconnect, server = source of truth for aggregated scores. | Evaluate PowerSync for offline-first sync. Current localStorage → Supabase migration already started. |
| 2026-03-21 | community | **29.4% complete digital mental health programs** — retention crisis is real. **What works**: peer support communities (moderated), group challenges ("7-day focus challenge"), anonymous mood sharing reduces stigma. **Avoid**: gamification without meaning — streaks/badges foster dependency not health. **BrainHealth Challenge model**: community-driven awareness + habit building. Combine human support with personalized adaptive interventions. | Design moderated community features: group challenges, anonymous mood sharing. No empty gamification. |
| 2026-03-21 | eeg-watch-fusion | **Feature-level fusion of EEG + HRV/EDA outperforms single-modality by 5-12%** (Neurocomputing 2025). Ensemble LSTM+GRU is SOTA. New framework handles sensor dropout gracefully (BLE disconnections). IDUN in-ear EEG = "smartwatch for the brain." **For NDW**: Already have health data flowing — need to wire HR/HRV into feature-level fusion alongside EEG, not just separate streams. | Wire Apple Watch HR/HRV into EEG feature pipeline for 5-12% accuracy boost. |
| 2026-03-21 | privacy-ml | **Federated learning + differential privacy for EEG achieves 92.6% accuracy** with minimal degradation vs centralized (ResearchGate 2025). Keeps raw EEG on-device. **On-device EEGNet**: 14.9ms inference, 0.76mJ per inference on mobile-class processor. **For NDW**: Add "privacy mode" that processes everything locally. Optional federated weight sharing (never raw data). | Implement privacy mode (all-local processing) + optional federated weight updates. |
| 2026-03-21 | new-datasets | **NEW: Consumer vs research-grade comparison dataset (2026)** — 30 participants, 4 consumer devices INCLUDING Muse 2, simultaneous with research-grade. First direct Muse benchmark. figshare.30162868. **EmoEEG-MC (2025)**: 60 participants, 7 emotions, video + imagery contexts. **For NDW**: Benchmark our emotion model against the Muse 2 data from this new dataset — first real validation opportunity. | Download consumer-grade benchmark dataset and validate EEGNet accuracy on real Muse 2 data. |
| 2026-03-21 | accessibility | **WCAG 2.2 + healthcare mandate May 2026** — brain health apps must meet WCAG AA. **EEG sonification (Stanford patent 2025)**: Transform brainwaves into audio for blind/low-vision users. SAS/IBM shipped AI sonification tools. **For NDW**: Add sonification mode for EEG (pitch = band power), ensure color displays pass WCAG AA contrast + work with color blindness, ARIA labels on all charts, keyboard-only calibration. | WCAG AA audit + add EEG sonification mode for accessibility. |
| 2026-03-21 | pediatric-eeg | **Alpha frequency is age-dependent**: 6.1 Hz at 6mo → 8.4 Hz at 5yr → 9.7 Hz at 13yr → 10.1 Hz adult. **Standard 8-13 Hz alpha band is WRONG for children.** **Emotional valence reversal**: Children show HIGH amplitude to positive stimuli, adults to negative. Adult emotion models systematically misclassify children's valence. **For NDW**: Must add age input and adjust frequency bands. Add warning if user <14 uses adult-calibrated alpha. | Add age-stratified frequency bands. Do NOT apply adult emotion models to children. |
| 2026-03-21 | battery-optimization | **EEG-over-BLE**: 337µA/channel with near-lossless compression + BLE 5.0 = 66+ days battery. **SmartAPM (Nature 2025)**: Deep RL adaptive power management extends battery 36% on wearables. **Muse gotcha**: Muse app holds BLE connection in background, blocking other apps. **For NDW**: Adaptive sampling rate (reduce when still), BLE connection manager for Muse app conflicts, battery impact estimation in settings. | Add adaptive sampling + BLE connection conflict handler for Muse. |
| 2026-03-21 | hormonal-eeg | **Alpha drops, theta rises around ovulation** (Archives of Women's Mental Health 2026). Emotion perception is cycle-dependent — brain responds most intensely to stimuli during follicular phase. Causal connectivity shifts across phases. **NDW already has menstrual tracking** — must wire cycle phase into EEG emotion baselines. "Follicular calibration" vs "luteal calibration" would reduce false positives. | Wire menstrual cycle phase into EEG emotion baseline adjustment. |
| 2026-03-21 | sleep-biomarkers | **Sleep spindle density predicts cardiovascular risk** (2025) — no consumer app surfaces this. Spindles have lowest between-night variability = reliable wearable biomarker. **Slow oscillation-spindle coupling** = memory consolidation quality marker, trackable over time. **OpenSpindleNet (2025)**: open-source deep learning spindle detection. **4-ch consumer EEG can report**: spindle density, spindle-SO coupling, slow wave activity — all beyond basic staging. | Add spindle density + SO coupling as advanced sleep biomarkers. Use OpenSpindleNet. |
| 2026-03-21 | substance-confounds | **Caffeine**: increases beta, decreases alpha — confounds relaxation readings (2025 wearable EEG study). **SSRIs**: decrease alpha/theta, increase beta/gamma. **Benzodiazepines**: paradoxically increase beta. **Cannabis**: greater response to negative faces. **ALL of these will be misinterpreted as emotional state changes.** App MUST ask about substance intake before interpreting results. Pre-session questionnaire, not optional. | Add pre-session substance/medication questionnaire. Adjust baselines accordingly. |
| 2026-03-21 | competitive-moat | **Muse's data moat**: 1B+ minutes EEG from 16M+ sessions. Built Foundational Brain Model. 200+ research studies. Muse S Athena = first EEG+fNIRS+SpO2 consumer device. **Neurable**: $35M Series A, "Intel Inside" play licensing BCI to headphone OEMs. **NDW's defensible moat**: (a) multimodal fusion (EEG+heart+sleep+hormones+substances) — no one combines all, (b) per-user personalization = switching costs, (c) biometric data flywheel. VCs want data accumulation + recurring revenue + FDA-adjacent claims. | Focus moat on multimodal data accumulation + personalization switching costs. |
| 2026-03-21 | clinical-validation | **JMIR meta-analysis (16 RCTs, 763 participants)**: Consumer neurofeedback shows only modest effect on distress (g=-0.16). No evidence for cognition/mindfulness/physiological improvement vs controls. Effects may be "neurosuggestion" (placebo). **Trial requirements**: sham-neurofeedback control, validated endpoints (PHQ-9, GAD-7, PSQI), EEG biomarker endpoints, 8+ weeks, n=100+/arm. **FDA 2026**: Stay "general wellness" = avoid 510(k) entirely. | Design sham-controlled validation study. Partner with university for IRB. |
| 2026-03-21 | b2b-enterprise | **Headspace for Employers** = full EAP replacement (therapy, psychiatry, coaching, mindfulness). The B2B playbook. **Muse**: 2000+ clinicians via Myndlift, alphabeats for performance. **Path**: employer wellness programs first (sell to HR), not insurance. Insurance needs RCTs + CPT codes = longer road. Employer wellness spend increasing (10% healthcare cost increase 2026). **Start B2B, build clinical evidence in parallel, then insurers.** | Design employer wellness offering. Target HR/benefits buyers. |
| 2026-03-21 | eeg-ai-agents | **Emotion-aware closed-loop systems are real** (2025) — AI integrates real-time EEG with adaptive interventions (breathing, music, notifications). Consumer devices shipping intervention triggers: Muse S Athena (audio), Flowtime (sound alerts), BrainBit NeuroFit (breathing). **For NDW**: Build "EEG-triggered intervention engine" — when stress crosses threshold, fire breathing exercise or playlist change. | Build intervention engine: stress threshold → auto-trigger breathing/music/notification. |
| 2026-03-21 | tpbm-eeg | **tPBM + EEG is actionable** — 808nm/1064nm light produces measurable beta/gamma EEG power changes (bioRxiv 2025). **Vielight Neuro Gamma 4** ($1,500, 810nm at 40Hz) integrates with EEG devices via Vie-AI. **Neuronic LIGHT** (March 2025, lower price, 1070nm). Effects vary by wavelength, pulse freq, skin tone. **For NDW**: Track pre/post EEG around tPBM sessions. Partner with Vielight or Neuronic. | Add tPBM session tracking — log pre/post EEG gamma power changes. |
| 2026-03-21 | vr-xr-eeg | **OpenBCI Galea** ($25K enterprise): EEG+EMG+EDA+PPG+eye-tracking in VR headset. **Apple Vision Pro testing affect detection** via eye-tracking + pupil dilation (no EEG). **Spatial-temporal transformer for VR emotion** (Frontiers 2025). **For NDW**: Future VR/XR layer could fuse EEG with eye-tracking. Track Apple's affect detection as competitive intel. | Document VR/XR emotion extension architecture for future. |
| 2026-03-21 | multilingual-voice | **SenseVoice (Alibaba, Apache 2.0)**: 400K+ hours, 50+ languages, 7x faster than Whisper, happy/sad/angry/neutral + audio events. **Cultural bias is real**: Japanese subtle cues vs Italian expressiveness. AI trained on Western data misreads East Asian emotional control. **Accuracy drops cross-culturally without adaptation.** **For NDW**: Integrate SenseVoice-Small. Add cultural-context parameter. Critical for global users. | Integrate SenseVoice-Small for multilingual voice emotion. Add cultural context setting. |
| 2026-03-21 | food-timing | **18-hour fast doesn't impair cognition** but theta increases under high load (PMC 2025). **Postprandial glucose spikes hurt working memory** (CGM study 2024). Low-GI meals → better attention at 170min post-meal. **Post-meal neural changes**: decreased limbic activity, increased prefrontal. **For NDW**: Correlate meal logs with EEG focus/drowsiness over next 2 hours. Flag food coma patterns. | Add meal timing → cognitive state correlation. Flag post-meal drowsiness from EEG. |
| 2026-03-21 | weather-mood | **EEG can predict seasonal mood decline** — SVM on summer EEG predicts winter SAD (Frontiers). SAD patients show lower delta/theta/alpha + frontal asymmetry. **Weather-mood link is context-dependent** — only works for people who spend time outdoors. **SAD affects 5% US population** (9-10% subsyndromal), 4:1 female:male. **For NDW**: Pull weather API, correlate with EEG mood. Build personal weather-mood model per user. | Add weather context layer — correlate local weather with EEG mood patterns. |
| 2026-03-21 | onnx-perf | **ONNX WASM on mobile: ~15ms inference for small models.** Default WASM binary 10.5MB (gzipped 6MB). Custom minimal build → ~3MB. INT8 quantized = 2-3x faster than FP32. InferenceSession.create() startup: 1-3s on mid-range phones. **ORT format (.ort) + custom build + INT8 = <1MB model + ~3MB runtime.** WebGPU available in Chrome 121+ for Android but NOT in Capacitor WebView. | Build custom minimal ONNX Runtime WASM with only required operators. Use ORT format models. |
| 2026-03-21 | supabase-rls | **RLS evaluates per-row** — calling auth.uid() per row is the main cost. **Fix**: wrap in subselect `(SELECT auth.uid())` for initPlan caching (called once, not per-row). Always index user_id columns. **Disable Realtime on high-write EEG tables** (each insert triggers N subscriber reads). **83% of exposed Supabase DBs involve RLS misconfigurations** (Jan 2025). Never use service_role key in client. | Enable RLS on all biometric tables. Use initPlan pattern. Disable Realtime on EEG tables. |
| 2026-03-21 | ble-reconnect | **Muse reconnection**: monitor onDisconnected → wait 1-2s → device.gatt.connect() → exponential backoff (max 3-5 attempts). **iOS**: JS execution pauses ~5min after backgrounding in WebView. Need bluetooth-central background mode. **Android 14+**: foreground service with visible notification required for background BLE. **Best plugin**: @capgo/capacitor-bluetooth-low-energy (free, background support). Capawesome BLE (premium) for headless tasks. | Implement exponential backoff reconnection + foreground service for Android background BLE. |
| 2026-03-21 | push-notifications | **Use @capacitor/local-notifications for daily reminders** (no server needed). Android 13+ requires POST_NOTIFICATIONS permission request. Android 12+ needs SCHEDULE_EXACT_ALARM for precise timing. **HIPAA critical: NEVER include PHI in notification body** — visible on lock screen = violation. Bad: "EEG shows elevated stress." Good: "You have a new insight." Generic text only, link to in-app content. Opt-in: Android 81%, iOS 51%. | Implement local scheduled notifications. Audit all notification text for HIPAA. |
| 2026-03-21 | eeg-compression | **Raw EEG: 2,048 bytes/sec = 7.2 MB/hour = 173 MB/day continuous.** Lossless: delta encoding + zstd = ~3-4x CR. For 80%+ reduction: downsample to 128Hz (halves data) + delta + zstd = ~85% reduction. Wavelet lossy: 5-10x CR with <1% distortion. **Strategy**: full resolution 24h, then compress for long-term. Multichannel: 2D wavelet on 4×N matrix exploits inter-channel correlation. | Implement delta encoding + zstd for EEG storage. Downsample to 128Hz for archival. |
| 2026-03-21 | apk-size | **Target: <25MB download** (every 6MB increase = 1% install drop-off). Baseline Capacitor: 8-15MB. ONNX WASM custom build: ~3MB. Models: <1MB. **Achievable: 15-18MB.** Key: R8 minification (30-40% savings), AAB splits by ABI, shrinkResources, custom ONNX build, ORT format. Alternative: serve WASM from CDN at runtime (removes from APK entirely). WebP/AVIF instead of PNG. | Build custom ONNX WASM, enable R8+shrinkResources, target <25MB AAB. |
| 2026-03-21 | freemium-pricing | **Freemium converts ~2-5%. Free trials convert 10-15% (40-60% with payment upfront).** Annual subs = 60.6% of health app revenue. Annual retention 33%. **Sweet spot**: 17-32 day free trial + annual pricing ~$40-50/year with visible discount vs monthly. Median pricing: $7.48/week, $12.99/month, $38.42/year. **For NDW**: 30-day free trial, then $49.99/year ($4.17/mo) or $9.99/month. | Design freemium tiers: free (basic wellness) + premium ($49.99/yr trial-based). |
| 2026-03-21 | aso-strategy | **70% of installs come from search.** List under Health & Fitness (not Medical). Target long-tail: "brain training meditation", "EEG mood tracker", "focus and calm." Apps below 3.5 stars get buried. Update screenshots 2-4x/year. Put strongest differentiator ("real-time brain data") in first two screenshots. **For NDW**: Combine feature keywords (EEG, brainwave, neurofeedback) with benefit keywords (focus, sleep, stress relief). | Create ASO keyword strategy + professional screenshots for Play Store + App Store. |
| 2026-03-21 | creator-marketing | **Micro-influencers (10K-100K) generate 22x higher conversion than macro.** CPI for wellness apps: $10.42 average. Best strategy: base rate + performance bonus. Target: meditation, biohacking, sleep optimization, neuroscience education creators. 73% of healthcare marketing budgets → social media. TikTok for instant trust, YouTube for depth. **For NDW**: Budget $5K for 10 micro-influencer pilots on TikTok/YouTube. | Plan micro-influencer campaign: 10 creators, TikTok + YouTube, $500 each + performance bonus. |
| 2026-03-21 | advisory-board | **SAB composition**: neuroscientist + psychiatrist + neuroethicist + FDA expert. Table stakes for investor confidence. Neurotech hit $4.8B/140 deals in 2025. **Neuroethics now mandatory** — UNESCO and OECD recommend ethics advisory for neurotech. INBRAIN template: Nobel laureate + AI expert + Stanford + bioethics. **For NDW**: Recruit 4-person SAB before fundraising. | Recruit SAB: neuroscientist, psychiatrist, neuroethicist, regulatory expert. |
| 2026-03-21 | patents | **6,500+ EEG/EMG patents in US.** Major holders: IBM, Microsoft, Samsung, Tencent. Most cover specific implementations, not broad concepts. Emotiv dominates research citations (67.7%). Neurable has 33 patents. **Patent filings doubling yearly.** Novel emotion detection methods are likely patentable. **For NDW**: File provisional patents on multimodal fusion pipeline + personal adapter. FTO analysis needed before shipping. | File provisional patent on multimodal fusion + adaptive personalization. Get FTO analysis. |
| 2026-03-21 | localization | **RTL support mandatory for Arabic/Hebrew/Urdu.** Emotion labels culturally loaded — "emotional well-being" works where "mental illness" doesn't. **Regulatory differences are the real blocker**: US (FDA/HIPAA), EU (GDPR+MDR), India (DPDP Act), China (PIPL+NMPA). PHQ-9 validated in 100+ languages. "Therapy"/"diagnosis" carry legal restrictions in many countries. **For NDW**: Start with English, add Hindi/Telugu/Spanish. Wellness framing avoids regulatory triggers globally. | Plan i18n: English first, then Hindi/Telugu/Spanish. Wellness-only framing for global compliance. |
| 2026-03-21 | real-world-noise | **EMG contamination dominates beta/gamma bands** — facial muscles (talking, frowning) produce millivolts vs EEG microvolts. Lab-to-real-world transfer is an acknowledged gap. ART outperforms ICA but is offline only. **For NDW**: Detect and FLAG low-confidence windows rather than force classification. Show "Signal too noisy" when EMG detected. Design for 5-min quiet sessions, not all-day wear. | Add EMG detection + "signal too noisy" fallback instead of forced classification. |
| 2026-03-21 | user-trust | **Showing confidence scores changes behavior dramatically** — physician AI override dropped from 87% to 33% with confidence display. "I don't know" builds trust, not weakness. 88% of product leaders expect trust frameworks as core differentiator by 2026. **For NDW**: Visible confidence meter on EVERY emotion reading. When uncertain, show "Not enough data" not a guess. | Add confidence meter to all emotion displays. Show uncertainty honestly. |
| 2026-03-21 | mood-tracking-outcomes | **NO robust evidence mood monitoring improves symptoms** (JMIR 2026 meta-analysis). **19% of studies report adverse events** including mood worsening and data obsession. **BUT**: mood monitoring + mindfulness intervention = significant improvement (2025 RCT). **Key insight: tracking alone may be neutral or harmful. Must pair with intervention.** **For NDW**: NEVER show mood data without actionable "what can I do?" Always pair data with intervention. | Every mood display MUST include an actionable intervention suggestion. |
| 2026-03-21 | headband-compliance | **27% stop using by week 13** (Muse-S clinical trial). Sessions drop from 9/week to 3/week. Usage minutes: 542→29. Personalized programs cut dropout by 50%. Battery = compliance killer. **For NDW**: Design for 5-min sessions to start. Progressive length after habit formation. Never require headband for core app — voice check-in is the daily habit, EEG is enhancement. | Default 5-min sessions. Headband enhances but doesn't gate the experience. |
| 2026-03-21 | open-source-eeg | **MNE-Python** = gold standard for server-side EEG processing. **BrainFlow** = best for device connectivity (TypeScript bindings exist but no WASM). **NeuroKit2** for quick biosignal processing. **YASA** for sleep staging. **Antropy** for entropy. **None run in-browser via WASM today.** For NDW: BrainFlow for connectivity, MNE/NeuroKit2 for backend processing. Browser EEG = our custom JS pipeline. | Evaluate YASA for sleep staging on backend. Keep custom JS pipeline for client. |
| 2026-03-21 | interventions | **Cognitive reappraisal**: strongest evidence (g=-0.69 for anxiety). EEG neurofeedback + reappraisal combo = most novel. **HRV biofeedback via smartphone**: validated in 2025 RCT (no external hardware). **PMR**: consistently effective for state anxiety. **For NDW**: Three intervention tiers — (1) breathing (no hardware), (2) reappraisal prompts (personalized to emotion), (3) EEG-guided neurofeedback (highest engagement, requires headband). | Implement 3-tier intervention: breathing → reappraisal → neurofeedback. Always offer after mood data. |
| 2026-03-21 | consent-ux | **Granular toggle pattern is the standard** — per-service biometric permissions (enable EEG but disable voice). Time-bound permissions emerging ("use voiceprint for 30 days"). "Reject All" must have equal visual weight as "Accept All" (2026 enforcement). Only 12% of emotion recognition deployments have compliant consent. **ISO/IEC 24443 (neurodata encryption + consent)** draft expected 2026. **For NDW**: Build granular consent dashboard with per-modality toggles + time limits. | Build biometric consent dashboard: per-modality toggles, time-bound, equal reject/accept weight. |
| 2026-03-21 | breathing-from-eeg | **Respiration-entrained oscillations ARE detectable in frontal EEG** (Clinical Neurophysiology 2023). Paced breathing changes EEG connectivity in 0-2 Hz band. Not BPM-accurate but sufficient for "is user doing slow breathing?" detection. **Muse's frontal placement is ideal** — respiratory artifacts are a feature here, not a bug. **For NDW**: Detect breathing state (deep/shallow/holding) from frontal EEG during meditation. No extra sensor needed. | Add breathing state detection from frontal EEG during meditation sessions. |
| 2026-03-21 | gamification-evidence | **Narrative framing outperforms points/badges** — Zombies Run, Habitica models. For meditation: story arc ("journey through a forest that grows with practice"). **Adaptive AI-driven challenges** are the 2025-26 frontier — personalized difficulty curves. RCT evidence thin for specific mechanics but achievement + purpose + feedback loops most promising. **For NDW**: Design narrative meditation journeys, not just streaks. Adaptive difficulty based on session history. | Design narrative-based meditation progression (story arc) instead of pure streaks. |
| 2026-03-21 | data-export | **EDF+ is the de facto standard for EEG export** — BIDS recommended, open, readable by MNE/EEGLAB. Apple Health has NO EEG type. **FHIR adoption negligible in consumer apps** (1.4%). **For GDPR portability**: self-service export in settings — EDF+ for raw signals, CSV for sessions, JSON for full account. **For NDW**: Add data export page: EDF+ raw EEG + CSV summaries + JSON account data. | Implement self-service data export: EDF+ for EEG, CSV for sessions, JSON for account. |
| 2026-03-21 | crash-analytics | **Sentry is the clear winner for Capacitor** — dedicated @sentry/capacitor SDK, first-class source map support for WebView JS + native crashes. **HIPAA BAA on Business tier ($26/mo+)**, updated Jan 2026. Firebase Crashlytics does NOT support JS source maps from WebView. Bugsnag has no Capacitor SDK and no public BAA. **For NDW**: Install @sentry/capacitor, upload source maps in build pipeline. | Install Sentry with @sentry/capacitor SDK. Enable HIPAA BAA on Business tier. |
| 2026-03-21 | eog-as-signal | **Eye blink artifacts predict drowsiness** — validated Nov 2025 paper. Blink morphology (amplitude, duration, inter-blink interval) → deep learning → accurate drowsiness prediction WITHOUT clean EEG. **MED algorithm**: Muse-specific blink detection from single channel. **Blink rate <15/min** = deep focus or drowsiness; combine with alpha power to disambiguate. **For NDW**: Dual pipeline — extract blink features BEFORE artifact removal, then clean signal for emotion. Blinks = free attention/alertness data. | Add blink-rate extraction as attention/alertness signal before artifact removal. |
| 2026-03-21 | pain-eeg | **614-person mega-analysis (Lancet eBioMedicine 2025): standard band-power biomarkers do NOT replicate for pain. Only connectivity features work.** Wearable 2-channel prefrontal EEG: 78% pain severity classification. XGBoost achieves 94.2% acute pain with 4ms latency. Migraine prediction via CNV amplitude rising day before attack. **For NDW**: Pain/headache tracking possible from frontal EEG but needs connectivity features, not just band powers. | Add pain/migraine tracking as future feature. Requires connectivity analysis (not just band power). |
| 2026-03-21 | voice-telehealth | **Kintsugi shut down Feb 2026, open-sourced ALL technology** — free IP goldmine. Validated against SCID-5 clinician interviews. **Ellipsis Health "Sage"**: $45M Series A, real-time adaptive voice AI agents for care management (Salesforce AppExchange Jan 2026). **Critical warning**: experts say AI cannot reliably assess suicidal ideation, homicidal ideation, NSSI, or delusions from voice alone. Human judgment required. **For NDW**: Use Kintsugi models for wellness ONLY. Never position as crisis detection. | Clone Kintsugi open-source repos. Frame as wellness indicators. Never claim crisis detection capability. |
| 2026-03-21 | digital-twin | **Digital Twin Brain (BME Frontiers 2025)**: Hypernetwork ingests resting-state connectome → generates personalized RNN → >90% accuracy predicting behavior, RT correlation >0.85. In silico interventions alter amygdala response. **Stanford digital twin neurons (Apr 2025)**: predict responses to entirely novel stimuli. **EBRAINS Virtual Brain Twins**: EU project for clinical neurology. **Still pre-consumer** but clinics piloting companion apps. | Long-term vision: personal brain model from accumulated EEG sessions. Document architecture now. |
| 2026-03-21 | creative-flow | **First field replication of flow EEG (CHI 2025)**: cEEGrid around-the-ear EEG during real academic writing + software engineering. 200 hours collected. Theta-flow quadratic relationship replicated outside lab. **Neurable MW75 Neuro LT ($499, shipping)**: 12 dry EEG sensors in headphones, targets programmers/creators, 33% focus increase. **For NDW**: Flow detection for knowledge workers is a validated use case with consumer hardware shipping. | Add "Deep Work" mode targeting programmers/creators. Flow detection from theta pattern. |
| 2026-03-21 | loneliness | **EEG: lonely brains show decreased beta + altered alpha connectivity + hypervigilance to social threats.** **Voice NLP: "I/me" vs "we/ours" pronoun patterns detect loneliness.** Feeling adjectives also discriminate. **CRITICAL WARNING (Harvard/MIT 2025): AI chatbot companions INCREASE loneliness at high usage.** Voice chatbots initially help but benefit vanishes. Heavy emotional disclosure to AI = lower well-being. **For NDW**: Detect loneliness patterns but direct users to REAL social connection, not more app usage. | Add loneliness indicators (pronoun analysis + EEG beta). But direct to social connection, NOT more app time. |
| 2026-03-21 | eeg-music-gen | **Transformer-based EEG-to-MIDI generation exists in lab** (Frontiers Neurorobotics 2024). AI sleep-aid music guided by EEG+HR in real-time (Frontiers Psychology 2026). **EEGsynth (open-source)**: converts real-time EEG to MIDI/OSC/analog synth. Used in live performances. **Brain.fm + Muse is NOT true EEG-adaptive** — music is pre-composed. No consumer closed-loop product yet. **For NDW**: EEGsynth integration could enable real-time brain-music generation. Unique feature no competitor has. | Evaluate EEGsynth for real-time brain-to-music generation feature. |
| 2026-03-21 | march-2026-papers | **New EEG emotion architectures (March 2026)**: Multilayer Graph Transformer (Frontiers), Attention-Driven Transformer with cross-dataset fine-tuning achieving 84% on SEED-IV (IEEE). **EEG-Pype**: new open-source GUI MNE-Python pipeline (PLOS Comp Bio, Apache 2.0). **BrainFlow v5.21.0** released Feb 25. No major new datasets. | Track Graph Transformer architecture for potential adoption. |
| 2026-03-21 | march-2026-products | **Perplexity Health launched March 19** — health AI integrating Apple Health + EHR from 1.7M providers + Fitbit/Ultrahuman/Withings. Ask health questions grounded in YOUR data. **Flow Neuroscience FL-100**: FDA-approved at-home tDCS, US launch Q2 2026, 58% remission rate (Nature Medicine). 55K users in Europe. **Muse app v51.1** (Feb 28). No new Emotiv hardware. | Monitor Perplexity Health as potential integration partner. Track Flow FL-100 US launch. |
| 2026-03-21 | march-2026-voice | **Google DeepMind acquired Hume AI's team** (Jan 22, still developing March) — infusing Gemini with emotional intelligence. Hume continues independently, projecting $100M revenue 2026. **Canary Speech at HIMSS26** (March 9-12): 40-second voice sample detects depression/anxiety/cognitive impairment/Parkinson's. Partnership with GlobalMed for US federal healthcare. **New ethics framework** for voice health AI (AJOB March 2026). | Track Google/Hume integration in Gemini. Evaluate Canary Speech 40-second model approach. |
| 2026-03-21 | march-2026-funding | **$400M+ raised in first 2 weeks of March 2026 alone.** Science Corp $230M (retinal BCI, $1.5B valuation). Cognito Therapeutics $105M (gamma neurostim for Alzheimer's). Eight Sleep $50M ($1.5B valuation). **China approved first commercial invasive BCI.** Paradromics completed first-in-human recording. **Emotion AI market: $4.71B in 2025 → $5.99B projected 2026.** | NDW is in a $6B and growing market. Document for investor materials. |
| 2026-03-21 | march-2026-regulatory | **FDA Jan 2026 guidance still rippling**: noninvasive wellness wearables can claim general wellness if avoiding disease claims. **EU AI Act August 2, 2026 deadline approaching** — high-risk systems must complete conformity assessments. EU Health Package actively revising MDR/IVDR. **For NDW**: 4.5 months to EU AI Act deadline. Must start compliance work NOW. | Begin EU AI Act conformity assessment immediately — 4.5 months to deadline. |
| 2026-03-21 | impl-supabase-rls | **Exact SQL**: `USING ((SELECT auth.uid()) = user_id)` — initPlan pattern. Always index user_id. Disable Realtime on high-write tables: `ALTER PUBLICATION supabase_realtime DROP TABLE biometrics`. Per-operation policies (SELECT/INSERT/UPDATE/DELETE). 83% of Supabase DBs misconfigured. | Copy-paste SQL in cycle 17 research output. Ready to apply. |
| 2026-03-21 | impl-sentry | **@sentry/capacitor + @sentry/react + @sentry/vite-plugin.** Source maps: `sourcemap: "hidden"` in vite.config.ts, sentryVitePlugin as LAST plugin. Manual upload after cap sync: `sentry-cli sourcemaps inject/upload ./android/app/src/main/assets/public`. HIPAA BAA on Business tier ($26/mo). | Copy-paste setup in cycle 17 output. Install + configure. |
| 2026-03-21 | impl-notifications | **@capacitor/local-notifications.** Android: SCHEDULE_EXACT_ALARM + POST_NOTIFICATIONS permissions. `schedule({ on: { hour, minute }, repeats: true, allowWhileIdle: true })` for daily. **Gotcha**: Android 14+ revokes exact alarm permission → deletes all scheduled notifications → must re-schedule on app launch. | Copy-paste code in cycle 17 output. Replace current notification strategy with local scheduled. |
| 2026-03-21 | impl-revenuecat | **@revenuecat/purchases-capacitor.** Platform-specific API keys. Offerings → Packages → purchasePackage(). Free trials configured in App Store Connect / Play Console, not in code. Check `entitlements.active['premium']` for access. **iOS**: enable In-App Purchase capability. **Android**: launchMode="singleTop". | Copy-paste setup in cycle 17 output. Ready to implement freemium. |
| 2026-03-21 | impl-edf-export | **EDF+ format**: 256-byte header + ns×256 signal headers + int16 little-endian data. No good JS write libraries exist — need custom TypeScript writer. Skeleton code in cycle 17 output. BIDS-recommended format. Apple Health has NO EEG type. FHIR adoption negligible (1.4%). | Use skeleton TypeScript EDF+ writer from cycle 17. Add to data export page. |
| 2026-03-21 | impl-posthog | **@capawesome/capacitor-posthog + posthog-js.** Config in capacitor.config.ts. capture() for events, screen() for page views, identify() for users. Feature flags, session replay, consent management (optOut/optIn). Mask all text inputs + images in replay for privacy. | Copy-paste setup in cycle 17 output. Install + instrument key events. |
<!-- Entries will be appended below by the research agent -->
